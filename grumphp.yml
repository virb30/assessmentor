grumphp:
    git_hook_variables:
        EXEC_GRUMPHP_COMMAND: 'docker-compose run -e XDEBUG_MODE=coverage -T app'
    stop_on_failure: true
    tasks: 
        phpunit:
            always_execute: true
    fixer:
        enabled: false
